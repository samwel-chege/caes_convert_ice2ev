import { createSlice, PayloadAction } from '@reduxjs/toolkit'


const intialState  = 0;
export const distanceSlice = createSlice({
    name:"distance",
    intialState,
    reducers:{
        
    }
})